-- Minimal placeholder spec — never executed because the e2e tests use the
-- fake Open Cloud server which returns canned envelopes regardless of the
-- uploaded place contents. Just needs to match the testMatch glob so the
-- workspace runner reaches backend dispatch.
return {}
